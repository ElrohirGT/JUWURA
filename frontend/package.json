{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "@biomejs/biome format --write ./src",
    "format:check": "@biomejs/biome format ./src",
    "lint": "@biomejs/biome lint --write ./src"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "vite": "^6.0.1"
  },
  "dependencies": {
    "vite-plugin-elm": "^3.0.1"
  }
}
